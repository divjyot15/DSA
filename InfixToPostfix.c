#include<stdio.h>
#include<stdlib.h>
#include<string.h>

struct Node
{
    char data;
    struct Node * next;
};

int precedence(){
     
}

struct Node * push(){

}

struct Node * pop(){

}

void main(){
    char infix[100];
    printf("enter the infix expression that you want to convert to infix:\n");
    scanf(" %s",infix);
    printf("The infix expression you entered is:\n%s\n",infix);
    char postfix[strlen(infix)+1];
    int j=0;
    for (int i = 0; infix[i]!=NULL; i++)
    {
        
    }
    
}